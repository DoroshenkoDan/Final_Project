{
  "version": 2,
  "builds": [
    {
      "src": "client/**",
      "use": "@vercel/static-build",
      "config": { "distDir": "client/.next" }
    },
    {
      "src": "server/**/*.js",
      "use": "@vercel/node"
    }
  ]
}